We analyse US gun murder data collected by the FBI.

download-data.R - downloads csv file to data directory

wrangle-data.R - creates a derived dataset an saves as R object in rda directory

analysis.R - generates a plot ans saves it to the figs directory